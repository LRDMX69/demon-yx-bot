# Dēmonyx local data

`moe-training.jsonl` is a small checked-in routing corpus used to train the local-first MoE gate. Runtime SaveSo data is written to `data/saveso.json` and is excluded from Git; never place personal notes, WhatsApp session data, or credentials in tracked files.
